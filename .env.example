# NextAuth Configuration
# Generate a random secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-random-secret-here-min-32-chars
NEXTAUTH_URL=http://localhost:3000

# Sistema - Credenciales de Admin
# Email del administrador
ADMIN_EMAIL=admin@example.com
# Contraseña del administrador
ADMIN_PASSWORD=change-this-password

# TMetric - Credenciales para scraping
# Usar las credenciales de tu cuenta TMetric
TMETRIC_EMAIL=tu-email@empresa.com
TMETRIC_PASSWORD=tu-password-tmetric

# Slack - Webhook para notificaciones
# Obtener desde: https://api.slack.com/apps → Tu App → Incoming Webhooks
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# API Secret para Railway Cron y ejecuciones automáticas
# Generar con: openssl rand -hex 32
# IMPORTANTE: Usar el mismo valor en tu servicio de cron (EasyCron, cron-job.org, etc.)
API_SECRET=your-api-secret-here-min-64-chars

# IMPORTANTE: NO configurar NODE_ENV manualmente
# Next.js lo configura automáticamente según el comando (dev/build/start)
# Si tienes NODE_ENV en tu shell, desactívalo antes de hacer build
# NODE_ENV=development
